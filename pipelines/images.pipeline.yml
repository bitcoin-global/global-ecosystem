spruce:
# Build pipeline file
- base: base.yml
  merge:
  - with_all_in: consts/
    regexp: ".*.(yml)"
  - with_all_in: jobs/shared/
    regexp: ".*.(yml)"
  - with_all_in: resources/shared/
    regexp: ".*.(yml)"
  - with:
      files:
      - resources/docker.builder.yml
      - resources/git.global-ecosystem.yml
  - with_all_in: jobs/builder/
    regexp: ".*.(yml)"
  to: ignore.builder.pipeline.yml

# Clean pipeline file
- base:  ignore.builder.pipeline.yml
  prune:
  - meta
  - meta_plan
  to: ignore.builder.pipeline.yml

# Deploy pipeline file
fly:
  target: bitglob
  name: docker-images
  config: ignore.builder.pipeline.yml
