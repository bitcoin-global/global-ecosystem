---
resources:
- name: bitcoin-infra
  type: git
  icon: github
  source:
    uri: (( grab meta.bitcoin-infra-repository ))
    username: ((git.username))
    password: ((git.password))
    branch: master
