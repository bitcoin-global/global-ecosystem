---
resources:
- name: global-gizmos
  type: git
  icon: github
  source:
    uri: (( grab meta.global-gizmos-repository ))
    username: ((git.username))
    password: ((git.password))
    branch: master
