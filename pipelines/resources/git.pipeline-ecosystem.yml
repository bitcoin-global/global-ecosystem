---
resources:
- name: pipeline-ecosystem
  type: git
  icon: github
  source:
    uri: (( grab meta.global-ecosystem-repository ))
    username: ((git.username))
    password: ((git.password))
    branch: master
    paths:  
    - pipelines