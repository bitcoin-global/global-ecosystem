---
resources:
- name: bitcoin-global
  type: git
  icon: github
  source:
    uri: (( grab meta.bitcoin-global-repository ))
    username: ((git.username))
    password: ((git.password))
    branch: (( grab meta.overrides.bitcoin-global.branch || "master" ))
